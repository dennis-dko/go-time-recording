{
  "openapi": "3.0.3",
  "info": {
    "title": "go-time-recording API",
    "version": "1.0.0",
    "description": "Project time tracking.\n\nAuthenticate either with the session cookie obtained from `POST /auth/login`, or with a personal API token sent as `Authorization: Bearer <token>` or `X-API-Token: <token>`.\n\nA token carries no rights of its own: every request is evaluated against the owner's **current** role, so changing or revoking a role takes effect on the very next call.\n\nAll responses are wrapped by the framework as `{\"data\": ...}` on success and `{\"error\": {\"message\": ...}}` on failure."
  },
  "servers": [
    {
      "url": "/api/v1"
    }
  ],
  "security": [
    {
      "bearerAuth": []
    },
    {
      "apiToken": []
    },
    {
      "sessionCookie": []
    }
  ],
  "tags": [
    {
      "name": "auth",
      "description": "Sign-in, sign-out and your own account"
    },
    {
      "name": "timesheets",
      "description": "Time entries"
    },
    {
      "name": "projects",
      "description": "Your own projects"
    },
    {
      "name": "users",
      "description": "User administration"
    },
    {
      "name": "roles",
      "description": "Roles and permissions"
    },
    {
      "name": "reports",
      "description": "Reports and overtime"
    },
    {
      "name": "settings",
      "description": "Instance administration, built-in administrator only"
    },
    {
      "name": "setup",
      "description": "First-run wizard"
    }
  ],
  "paths": {
    "/auth/login": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Sign in",
        "security": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "email",
                  "password"
                ],
                "properties": {
                  "email": {
                    "type": "string",
                    "format": "email"
                  },
                  "password": {
                    "type": "string",
                    "format": "password"
                  },
                  "totp": {
                    "type": "string",
                    "description": "Required when the account has two-factor authentication enabled."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Signed in; a session cookie is set. When the response says `totpRequired`, no cookie is set and the code must be supplied."
          },
          "401": {
            "description": "Invalid credentials"
          },
          "429": {
            "description": "Too many attempts"
          }
        }
      }
    },
    "/auth/passkey": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "Whether passkeys can be used on this connection",
        "description": "Browsers expose WebAuthn only on a secure context - HTTPS, or localhost - and reject an IP address as a relying party identifier. The interface asks before offering a button that could only fail with \"SecurityError: This is an invalid domain\".",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    },
    "/auth/passkey/login": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Start a passwordless sign-in",
        "description": "Returns a challenge. No username is asked for: the browser offers whichever credentials it holds for this site, and the one that signs names its own owner.",
        "responses": {
          "200": {
            "description": "Challenge"
          }
        }
      },
      "put": {
        "tags": [
          "auth"
        ],
        "summary": "Finish a passwordless sign-in",
        "description": "Verifies the signature and opens a session, exactly as a password sign-in does - same cookie, same lifetime, same CSRF rotation. Every failure answers alike.",
        "responses": {
          "201": {
            "description": "Signed in"
          }
        }
      }
    },
    "/auth/logout": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Sign out and end the session",
        "responses": {
          "201": {
            "description": "Signed out"
          }
        }
      }
    },
    "/me": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "The signed-in user and their permissions",
        "responses": {
          "200": {
            "description": "Identity and permissions"
          },
          "401": {
            "description": "Not authenticated"
          }
        }
      }
    },
    "/me/password": {
      "put": {
        "tags": [
          "auth"
        ],
        "summary": "Change your own password; ends every session you have",
        "responses": {
          "200": {
            "description": "Changed"
          },
          "400": {
            "description": "Wrong current password, or the new one is too short"
          }
        }
      }
    },
    "/me/language": {
      "put": {
        "tags": [
          "auth"
        ],
        "summary": "Set your interface language",
        "responses": {
          "200": {
            "description": "Saved"
          },
          "400": {
            "description": "Unsupported language"
          }
        }
      }
    },
    "/me/theme": {
      "put": {
        "tags": [
          "me"
        ],
        "summary": "Set the appearance you read in, or clear it to follow the time of day",
        "description": "\"light\", \"dark\", or an empty value. Empty is meaningful rather than an omission: it is how somebody goes back to the screen following the time of day. The appearance is stored on the account rather than in the browser, so a shared machine does not hand the next person the last one's.",
        "responses": {
          "200": {
            "description": "Saved"
          },
          "400": {
            "description": "Unknown appearance"
          }
        }
      }
    },
    "/me/timezone": {
      "put": {
        "tags": [
          "me"
        ],
        "summary": "Set your own timezone, or clear it to follow the instance",
        "description": "An IANA name such as \"Europe/Berlin\". An empty value is meaningful: it clears the personal setting so the account follows the instance-wide zone again. An unknown name is rejected rather than stored, because it would otherwise fall back to UTC at every use and move bookings between days.",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/me/tour": {
      "put": {
        "tags": [
          "me"
        ],
        "summary": "Record whether you have been shown the guided tour",
        "description": "Stored on the user rather than in the browser, so someone who has been introduced to the application is not introduced again on their next device. Settable both ways: sending false offers the tour again, which is how the \"Show the tour\" button works.",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/me/passkeys": {
      "get": {
        "tags": [
          "me"
        ],
        "summary": "List your registered passkeys",
        "description": "Carries no key material. The public half could be sent harmlessly - it verifies signatures and cannot produce them - but sending it invites building something on a value that is meaningless outside a ceremony.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    },
    "/me/passkeys/register": {
      "post": {
        "tags": [
          "me"
        ],
        "summary": "Start registering a passkey",
        "description": "Refused for the built-in administrator: it is the guaranteed way back into an installation, and a guarantee that depends on a particular device still existing is not one.",
        "responses": {
          "200": {
            "description": "Challenge"
          }
        }
      },
      "put": {
        "tags": [
          "me"
        ],
        "summary": "Finish registering a passkey",
        "description": "Verifies what the authenticator created and stores the public half.",
        "responses": {
          "200": {
            "description": "Registered"
          }
        }
      }
    },
    "/me/passkeys/{id}": {
      "delete": {
        "tags": [
          "me"
        ],
        "summary": "Revoke a passkey",
        "description": "Scoped to the owner, so an id from somewhere else deletes nothing. Allowed even for the last one: a passkey is an addition to the password, never a replacement, so removing it cannot lock anyone out.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    },
    "/me/totp": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Begin two-factor enrolment; the secret is returned once",
        "responses": {
          "201": {
            "description": "Secret and otpauth URI"
          }
        }
      },
      "put": {
        "tags": [
          "auth"
        ],
        "summary": "Confirm two-factor with a current code",
        "responses": {
          "200": {
            "description": "Enabled"
          },
          "400": {
            "description": "Invalid code"
          }
        }
      },
      "delete": {
        "tags": [
          "auth"
        ],
        "summary": "Disable two-factor",
        "parameters": [
          {
            "name": "code",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Disabled"
          },
          "400": {
            "description": "Invalid code"
          }
        }
      }
    },
    "/me/tokens": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "List your API tokens; secrets are never returned",
        "responses": {
          "200": {
            "description": "Tokens"
          }
        }
      },
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Issue an API token; the secret is shown once and cannot be recovered",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "name"
                ],
                "properties": {
                  "name": {
                    "type": "string"
                  },
                  "expiresInDays": {
                    "type": "integer",
                    "description": "0 means the token does not expire on its own."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The token, including its secret"
          },
          "409": {
            "description": "Token limit reached"
          }
        }
      }
    },
    "/me/tokens/{id}": {
      "delete": {
        "tags": [
          "auth"
        ],
        "summary": "Revoke one of your own tokens",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Revoked"
          },
          "404": {
            "description": "Not one of your tokens"
          }
        }
      }
    },
    "/timesheets": {
      "get": {
        "tags": [
          "timesheets"
        ],
        "summary": "List time entries",
        "description": "Your own entries. `userId` may name you, which is a reasonable thing for a client to send; naming anybody else is refused rather than quietly answered with your own rows, because a list that does not match what was asked for is worse than a plain no.",
        "parameters": [
          {
            "name": "userId",
            "in": "query",
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "projectId",
            "in": "query",
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "from",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Entries"
          }
        }
      },
      "post": {
        "tags": [
          "timesheets"
        ],
        "summary": "Book time",
        "description": "`projectId` is optional; omitting it records uncategorised hours. Omitting `userId` books for the caller.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TimesheetInput"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created"
          },
          "409": {
            "description": "Daily limit exceeded, or the project no longer accepts entries"
          }
        }
      }
    },
    "/timesheets/{id}": {
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "required": true,
          "schema": {
            "type": "integer"
          }
        }
      ],
      "get": {
        "tags": [
          "timesheets"
        ],
        "summary": "Read one entry",
        "responses": {
          "200": {
            "description": "Entry"
          }
        }
      },
      "put": {
        "tags": [
          "timesheets"
        ],
        "summary": "Update an entry",
        "description": "Changes an entry of your own. Somebody else’s is refused, and there is no right that changes that - including naming somebody else in `userId`, which would hand the entry over. A project given here goes through the same checks as booking onto it: it must exist, be one you may see, and still accept hours.",
        "responses": {
          "200": {
            "description": "Updated"
          },
          "409": {
            "description": "The day would go over its ceiling, or the project no longer accepts hours"
          }
        }
      },
      "delete": {
        "tags": [
          "timesheets"
        ],
        "summary": "Delete an entry",
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/timesheets/{id}/transfer": {
      "post": {
        "tags": [
          "timesheets"
        ],
        "summary": "Move an entry to another project",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Moved"
          },
          "409": {
            "description": "The entry is already on that project, or the project is closed for booking"
          }
        }
      }
    },
    "/projects": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "List projects",
        "description": "Your own projects, which is all there are. There were two kinds - a shared project everybody could see and a private category - and a `private=true` parameter to narrow to the latter; every project belongs to one person now, so there is nothing to narrow. Needs `projects:read`.",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Projects"
          }
        }
      },
      "post": {
        "tags": [
          "projects"
        ],
        "summary": "Create a project",
        "description": "The project belongs to whoever created it, always. There was a `private: true` to ask for that, and `projects:write:own` beside `projects:write` to decide which kind you were allowed to make; there is one kind, so neither is left. `startDate` is optional - a project is one person’s way of organising their own hours, not a plan. Needs `projects:write`.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProjectInput"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created"
          },
          "403": {
            "description": "Missing permission"
          }
        }
      }
    },
    "/projects/{id}": {
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "required": true,
          "schema": {
            "type": "integer"
          }
        }
      ],
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "Read one project",
        "responses": {
          "200": {
            "description": "Project"
          },
          "404": {
            "description": "Unknown, or not yours - which answers the same way, so asking cannot tell you what colleagues have"
          }
        }
      },
      "put": {
        "tags": [
          "projects"
        ],
        "summary": "Update a project",
        "responses": {
          "200": {
            "description": "Updated"
          }
        }
      },
      "delete": {
        "tags": [
          "projects"
        ],
        "summary": "Delete a project",
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "409": {
            "description": "The project still has time entries"
          }
        }
      }
    },
    "/projects/{id}/archive": {
      "post": {
        "tags": [
          "projects"
        ],
        "summary": "Archive a completed project",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Archived"
          },
          "409": {
            "description": "Not completed, or open entries remain"
          }
        }
      }
    },
    "/projects/{id}/report": {
      "get": {
        "tags": [
          "reports"
        ],
        "summary": "Hours booked on a project",
        "description": "Your own hours on the project, and nobody else’s. It used to be a breakdown per colleague, gated on a separate `reports:read` that no role held, so the screen was unreachable and would have shown what each colleague had booked. Needs `reports:read:own`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "from",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Report"
          },
          "403": {
            "description": "Missing permission"
          }
        }
      }
    },
    "/reports": {
      "get": {
        "tags": [
          "reports"
        ],
        "summary": "Hours you booked, with or without a project",
        "description": "Your own hours over a period. `projectId` chooses what it covers: leave it out for every project, send `none` for the hours that were never given one, or send an id for that project. The project-scoped report at `/projects/{id}/report` cannot express either of the first two, because an id has no spare value for \"all\" and \"none\" at once. Needs `reports:read:own`.",
        "parameters": [
          {
            "name": "projectId",
            "in": "query",
            "description": "A project id, `none` for the entries without a project, or omitted for all.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "from",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Report"
          },
          "400": {
            "description": "Unreadable project selection, or a range that ends before it starts"
          },
          "403": {
            "description": "Missing permission"
          }
        }
      }
    },
    "/users/{id}/overtime": {
      "get": {
        "tags": [
          "reports"
        ],
        "summary": "Overtime balance for one user",
        "description": "Your own balance, which needs `reports:read:own`. Anyone else’s is refused outright: a balance is their recorded time, totalled, and nobody reads that. Only days with bookings count.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "from",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Balance"
          },
          "403": {
            "description": "Missing permission"
          }
        }
      }
    },
    "/users": {
      "get": {
        "tags": [
          "users"
        ],
        "summary": "List users",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Users"
          }
        }
      },
      "post": {
        "tags": [
          "users"
        ],
        "summary": "Create a user",
        "responses": {
          "201": {
            "description": "Created"
          },
          "409": {
            "description": "Email already in use"
          }
        }
      }
    },
    "/users/{id}": {
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "required": true,
          "schema": {
            "type": "integer"
          }
        }
      ],
      "get": {
        "tags": [
          "users"
        ],
        "summary": "Read a user; your own account needs no permission",
        "responses": {
          "200": {
            "description": "User"
          }
        }
      },
      "put": {
        "tags": [
          "users"
        ],
        "summary": "Update a user",
        "responses": {
          "200": {
            "description": "Updated"
          }
        }
      },
      "delete": {
        "tags": [
          "users"
        ],
        "summary": "Delete a user",
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "409": {
            "description": "The built-in administrator cannot be deleted"
          }
        }
      }
    },
    "/users/{id}/role": {
      "put": {
        "tags": [
          "users"
        ],
        "summary": "Assign a role",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Assigned"
          },
          "409": {
            "description": "The built-in administrator must keep a role that can administer"
          }
        }
      }
    },
    "/users/{id}/password": {
      "put": {
        "tags": [
          "users"
        ],
        "summary": "Reset an account password",
        "description": "Gives an account a password chosen by whoever holds users:write, for letting somebody back in who has forgotten theirs. The account is flagged to replace it before it can use anything, and every session it has open ends. Refused for the caller’s own account, which is changed through /me/password with the current password, and for a directory account, whose password lives in the directory.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "password"
                ],
                "properties": {
                  "password": {
                    "type": "string",
                    "minLength": 8
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Reset"
          },
          "400": {
            "description": "The password is too short"
          },
          "409": {
            "description": "Your own account, or one kept in the directory"
          }
        }
      }
    },
    "/users/{id}/working-times": {
      "put": {
        "tags": [
          "users"
        ],
        "summary": "Set your own daily target and daily maximum",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Saved"
          },
          "400": {
            "description": "The target cannot exceed the maximum"
          },
          "403": {
            "description": "Somebody else's account, or the right is not held"
          }
        },
        "description": "Yours alone. Needs `settings:write:own`, and the id has to be your own: there is no wider permission that reaches somebody else’s, because a daily target is a time figure and everything to do with time belongs to the person it is about. An empty value, sent as 0, means \"follow the instance default\" under Settings. The account that administers this installation cannot set these for anybody, which is deliberate - it cannot read the entries, the balance or the figures the numbers produce."
      }
    },
    "/roles": {
      "get": {
        "tags": [
          "roles"
        ],
        "summary": "List roles",
        "responses": {
          "200": {
            "description": "Roles"
          }
        }
      },
      "post": {
        "tags": [
          "roles"
        ],
        "summary": "Create a role",
        "responses": {
          "201": {
            "description": "Created"
          },
          "400": {
            "description": "Unknown permission"
          }
        },
        "description": "A role has to grant at least one permission: one that grants nothing can be assigned, and whoever holds it signs in to an interface with nothing on it."
      }
    },
    "/roles/{id}": {
      "parameters": [
        {
          "name": "id",
          "in": "path",
          "required": true,
          "schema": {
            "type": "integer"
          }
        }
      ],
      "get": {
        "tags": [
          "roles"
        ],
        "summary": "Read a role",
        "responses": {
          "200": {
            "description": "Role"
          }
        }
      },
      "put": {
        "tags": [
          "roles"
        ],
        "summary": "Update a role",
        "responses": {
          "200": {
            "description": "Updated"
          },
          "409": {
            "description": "System roles cannot be renamed or weakened"
          }
        },
        "description": "A system role may be described differently and nothing else: not renamed, and its permissions not changed in either direction. That role belongs to the built-in administrator, which configures the installation and records no time — a right added here would hand it a working day from the screen that administers roles. Somebody who should both work here and administer is given the user-admin role instead."
      },
      "delete": {
        "tags": [
          "roles"
        ],
        "summary": "Delete a role",
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "409": {
            "description": "System role, or the role is still assigned"
          }
        }
      }
    },
    "/permissions": {
      "get": {
        "tags": [
          "roles"
        ],
        "summary": "Every permission the application enforces",
        "responses": {
          "200": {
            "description": "Permissions"
          }
        }
      }
    },
    "/languages": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "Supported interface languages",
        "security": [],
        "responses": {
          "200": {
            "description": "Languages"
          }
        }
      }
    },
    "/branding": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Instance title, logo and footer",
        "security": [],
        "description": "Public on purpose: the sign-in screen needs it before anyone is authenticated.",
        "responses": {
          "200": {
            "description": "Branding"
          }
        }
      }
    },
    "/settings/branding": {
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Save branding",
        "responses": {
          "200": {
            "description": "Saved"
          },
          "403": {
            "description": "Built-in administrator only"
          }
        }
      }
    },
    "/setup": {
      "get": {
        "tags": [
          "setup"
        ],
        "summary": "What a fresh installation still has to settle",
        "description": "Each step reports whether it is done, worked out from what is actually configured rather than from a record of the wizard having been shown - so a setting that is undone makes its step outstanding again. Two steps are required (the administrator password and the timezone); the rest are optional. Built-in administrator only, because a list of what is not yet configured is a useful thing for an attacker to read. Deliberately reachable while the initial password blocks the rest of the API.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    },
    "/setup/complete": {
      "post": {
        "tags": [
          "setup"
        ],
        "summary": "Dismiss the wizard",
        "description": "Records that the wizard was dismissed, which settles the optional steps only. It reappears by itself while any required step is outstanding, so this cannot be used to run an installation without a timezone.",
        "responses": {
          "201": {
            "description": "Dismissed"
          }
        }
      }
    },
    "/settings/operational": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the operational limits",
        "description": "Returns three things: the values an administrator has overridden, the values currently in force, and the values the configuration file supplies. An absent field under \"configured\" means that limit still follows the file.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Set the operational limits",
        "description": "Send only the fields to override; omitting one makes it follow the configuration file again, and an empty object resets everything. Values are bounded on save, because this screen can otherwise lock out its own administrator. Takes effect within seconds, without a restart. Administrator only.",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/settings/timezone": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the instance-wide timezone",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Set the instance-wide timezone",
        "description": "Decides which calendar day a booking falls on for every user who has not set their own. Administrator only.",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/settings/datasource": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the stored database connection; the password is never returned",
        "responses": {
          "200": {
            "description": "Connection"
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Save the database connection; applied on the next restart",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/settings/telemetry": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the logging, metrics and tracing settings",
        "description": "Returns what has been administered under \"configured\" and what this process is actually writing, serving and exporting under \"active\". The two disagree until the next restart. An absent field under \"configured\" follows the configuration file; \"traceExporter\": \"\" is an administered off, which is a different thing. Administrator only.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Set the logging, metrics and tracing settings; applied on the next restart",
        "description": "Send only the fields to administer; an empty object makes everything follow the configuration file again. \"logLevel\" is DEBUG, INFO, NOTICE, WARN, ERROR or FATAL. \"traceExporter\" is \"otlp\", \"jaeger\" or \"\" for off, and needs \"tracerUrl\" as a bare host:port when it names an exporter - a scheme is read as part of the host name and exports nothing. \"metricsOff\": true stops the metrics endpoint being served; the port itself stays in the configuration file, because GoFr refuses to start when something already holds it. Applied when the application is restarted: GoFr reads the level, binds the port and builds the exporter while starting up. Administrator only.",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/settings/datasource/test": {
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Probe a database connection without saving or switching to it",
        "responses": {
          "201": {
            "description": "Result; the `ok` field tells success from failure"
          }
        }
      }
    },
    "/settings/ldap": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the LDAP configuration; the bind password is never returned",
        "responses": {
          "200": {
            "description": "Configuration"
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Save the LDAP configuration; takes effect immediately",
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      }
    },
    "/settings/ldap/test": {
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Probe an LDAP connection without saving it",
        "responses": {
          "201": {
            "description": "Result; the `ok` field tells success from failure"
          }
        }
      }
    },
    "/me/timer": {
      "get": {
        "tags": [
          "me"
        ],
        "summary": "Is a clock running, and since when",
        "description": "Answers {\"running\": false} when nothing is going. While one runs it carries startedAt, so a client can count up on its own rather than polling, and elapsedHours, which is what the entry would record - a browser with a wrong clock then shows a slightly wrong number and still books the right one.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      },
      "post": {
        "tags": [
          "me"
        ],
        "summary": "Start the clock",
        "description": "Body is optional: {\"projectId\": 3, \"description\": \"...\"}, both of which may be left out and settled later. Starting again replaces a clock already running - somebody who does that has changed their mind about what they are doing, and refusing would leave them to stop a clock measuring the wrong thing. Always the caller's own clock; there is no route for anybody else's.",
        "responses": {
          "200": {
            "description": "Running"
          },
          "201": {
            "description": "Running"
          }
        }
      },
      "delete": {
        "tags": [
          "me"
        ],
        "summary": "Discard the clock, recording nothing",
        "description": "The way out of a clock nobody meant to start.",
        "responses": {
          "204": {
            "description": "Discarded"
          }
        }
      }
    },
    "/me/timer/stop": {
      "post": {
        "tags": [
          "me"
        ],
        "summary": "Stop the clock and book what it measured",
        "description": "Creates an ordinary time entry through the same service a typed booking goes through, so it meets the same rules: the daily cap, the project having to exist, be active and be visible, the description length. The duration is the measured one, to the second and rounded to nothing. The entry lands on the day the clock was *started*, in the caller's own timezone - somebody who starts at half past eleven at night and stops after midnight worked that evening.\n\nRefused when the clock has run for less than the smallest bookable duration (discard it instead) or for more than one entry can hold, which is almost always a clock somebody forgot; the measured hours are in that message so it can be booked by hand. A refusal leaves the clock running, so the time is not lost to it.",
        "responses": {
          "200": {
            "description": "The entry that was created"
          },
          "201": {
            "description": "The entry that was created"
          }
        }
      }
    },
    "/me/statistics": {
      "get": {
        "tags": [
          "me"
        ],
        "summary": "What your own recorded time adds up to",
        "description": "Totals the caller's own entries: per day, per project, and per workflow state, with the overall total. Range as ?from=YYYY-MM-DD&to=YYYY-MM-DD, inclusive at both ends; the default is the first of the current month to today, worked out in the caller's own timezone.\n\nDeliberately not the project report. That one is keyed on a project id, needs reports:read - which both default roles are without, so only the built-in administrator can see what other people total up to - and cannot express \"every project\" or \"no project\". This one is keyed on the caller and needs only timesheets:read:own.\n\nTwo details a chart depends on. Every day in the range is present, including the ones with nothing on them: a chart drawn only from the days that have entries shows a full week where there were two working days. And the entries with no project are their own row with projectId null, because \"not categorised\" is an answer worth seeing the size of rather than a gap. Rejected entries are left out, the same reading the overtime balance takes.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    },
    "/exports/document": {
      "post": {
        "tags": [
          "reports"
        ],
        "summary": "Export an evaluation as PDF",
        "description": "Lays out what an evaluation screen is showing and hands it back as a PDF: the report, the statistics, or an overtime calculation. Everything in the document arrives in the request, including the chart, which is drawn in the browser and sent as a PNG data URI - the chart in the file is the chart that was on the screen, in whichever of bars, columns or pie was chosen. Nothing is looked up, so this needs a session and no permission beyond it: there is no data here to be allowed or refused.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "A PDF document",
            "content": {
              "application/pdf": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "400": {
            "description": "The document is missing a title, or is larger than one document may be"
          },
          "401": {
            "description": "No session"
          }
        }
      }
    },
    "/timesheets/export": {
      "get": {
        "tags": [
          "timesheets"
        ],
        "summary": "Export time entries as .xlsx",
        "description": "Takes the same filters as the list, with the same scoping: whoever may read only their own entries exports their own, whatever they ask for.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "responses": {
          "200": {
            "description": "An .xlsx workbook",
            "content": {
              "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          }
        }
      }
    },
    "/timesheets/import": {
      "post": {
        "tags": [
          "timesheets"
        ],
        "summary": "Import time entries from .xlsx",
        "description": "Creates entries; it never changes or replaces existing ones. Every row goes through the same validation, daily ceiling and project visibility as a single booking. Every row is reported either way: the ones that would be written and the ones that cannot, with the reason. Nothing is written while any row is refused - a file half imported leaves somebody looking for which half.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": [
                  "file"
                ],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "The .xlsx workbook to read."
                  },
                  "dryRun": {
                    "type": "string",
                    "enum": [
                      "true",
                      "false"
                    ],
                    "description": "`true` previews without writing anything. Send the file twice - once to look, once to write - rather than relying on a plan held between requests."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "What the file would do, or what it did"
          },
          "201": {
            "description": "What the file would do, or what it did"
          },
          "400": {
            "description": "Not a readable workbook of this kind"
          },
          "409": {
            "description": "Some rows cannot be written, so nothing was"
          }
        }
      }
    },
    "/projects/export": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "Export projects as .xlsx",
        "description": "Your own projects, the same scoping as the list, so an export can never show more than the screen.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "responses": {
          "200": {
            "description": "An .xlsx workbook",
            "content": {
              "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          }
        }
      }
    },
    "/projects/import": {
      "post": {
        "tags": [
          "projects"
        ],
        "summary": "Import projects from .xlsx",
        "description": "Rows are matched by name: a name that exists is updated, a new one is created, so importing the same file twice changes nothing the second time. Every row becomes a project of the importer’s own, which no colleague can see - there was a Category column and a shared kind, and neither remains. An empty cell leaves that value as it is. The status is only written where it differs, so re-importing an export holding an archived project does not walk into the rule that archiving is only allowed from `completed`; asking for a status change the rule forbids is refused in the preview. Every row is reported either way: the ones that would be written and the ones that cannot, with the reason. Nothing is written while any row is refused - a file half imported leaves somebody looking for which half.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": [
                  "file"
                ],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "The .xlsx workbook to read."
                  },
                  "dryRun": {
                    "type": "string",
                    "enum": [
                      "true",
                      "false"
                    ],
                    "description": "`true` previews without writing anything. Send the file twice - once to look, once to write - rather than relying on a plan held between requests."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "What the file would do, or what it did"
          },
          "201": {
            "description": "What the file would do, or what it did"
          },
          "400": {
            "description": "Not a readable workbook of this kind"
          },
          "409": {
            "description": "Some rows cannot be written, so nothing was"
          }
        }
      }
    },
    "/users/export": {
      "get": {
        "tags": [
          "users"
        ],
        "summary": "Export accounts as .xlsx",
        "description": "Needs `users:read`. No password is included, in either direction.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "responses": {
          "200": {
            "description": "An .xlsx workbook",
            "content": {
              "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          }
        }
      }
    },
    "/users/import": {
      "post": {
        "tags": [
          "users"
        ],
        "summary": "Import account changes from .xlsx",
        "description": "Changes existing accounts, matched on the email address, and does not create them: a new account needs a password, and a password in a spreadsheet is a password that gets mailed around. An empty cell leaves that setting as it is. Needs `users:write`. Every row is reported either way: the ones that would be written and the ones that cannot, with the reason. Nothing is written while any row is refused - a file half imported leaves somebody looking for which half.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": [
                  "file"
                ],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "The .xlsx workbook to read."
                  },
                  "dryRun": {
                    "type": "string",
                    "enum": [
                      "true",
                      "false"
                    ],
                    "description": "`true` previews without writing anything. Send the file twice - once to look, once to write - rather than relying on a plan held between requests."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "What the file would do, or what it did"
          },
          "201": {
            "description": "What the file would do, or what it did"
          },
          "400": {
            "description": "Not a readable workbook of this kind"
          },
          "409": {
            "description": "Some rows cannot be written, so nothing was"
          }
        }
      }
    },
    "/roles/export": {
      "get": {
        "tags": [
          "roles"
        ],
        "summary": "Export roles as .xlsx, a column per permission",
        "description": "Needs `roles:read`. Every permission the application enforces gets a column holding yes or no, rather than one cell listing them: a typo in a list is a right silently dropped, and nothing about it looks wrong until somebody cannot open a screen. The permission columns are not translated - a permission is an identifier, and the role editor shows it verbatim.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "responses": {
          "200": {
            "description": "An .xlsx workbook",
            "content": {
              "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          }
        }
      }
    },
    "/roles/import": {
      "post": {
        "tags": [
          "roles"
        ],
        "summary": "Import roles from .xlsx",
        "description": "Creates and changes roles, matched on the name. Needs `roles:write`. A permission column is read by its heading rather than by position, so a file exported before a right existed still imports correctly; a heading naming a permission this application does not enforce is refused, because it means the file came from somewhere else. A system role may be described differently and nothing else - its permissions are what keeps the installation administrable. Every row is reported either way, and nothing is written while any row is refused.",
        "parameters": [
          {
            "name": "lang",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Language for the column headings and the values inside them, e.g. `de`. Unknown or absent means English. The importer reads a file in any of these languages whatever this was set to, because the heading row is skipped by position and translated values are recognised in every language."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": [
                  "file"
                ],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "The .xlsx workbook to read."
                  },
                  "dryRun": {
                    "type": "string",
                    "enum": [
                      "true",
                      "false"
                    ],
                    "description": "`true` previews without writing anything. Send the file twice - once to look, once to write - rather than relying on a plan held between requests."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "What the file would do, or what it did"
          },
          "201": {
            "description": "What the file would do, or what it did"
          },
          "400": {
            "description": "Not a readable workbook of this kind, or a permission column naming a right this application does not enforce - that one answers `unknownPermissionColumn` with the offending heading, since the file itself reads perfectly well"
          },
          "409": {
            "description": "Some rows cannot be written, so nothing was"
          }
        }
      }
    },
    "/maintenance": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Whether the installation is in maintenance mode",
        "description": "Public, like the branding and for the same reason: somebody opening the page during maintenance should read the notice on the sign-in screen rather than watch requests fail silently.",
        "responses": {
          "200": {
            "description": "The maintenance state and its notice"
          }
        }
      }
    },
    "/settings/maintenance": {
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Turn maintenance mode on or off",
        "description": "While it is on, everything but signing in and the settings themselves is refused, so an administrator cannot lock themselves out of turning it off again.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "enabled": {
                    "type": "boolean"
                  },
                  "message": {
                    "type": "string",
                    "description": "Shown on the sign-in screen."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The state as it now stands"
          }
        }
      }
    },
    "/settings/restart": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "What is waiting for a restart",
        "description": "Lists the stored settings the running process does not match yet. `startedAt` changes when the process does, which is how a client can tell a restart it asked for actually happened.",
        "responses": {
          "200": {
            "description": "Pending changes and the start time"
          }
        }
      },
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Restart the process",
        "description": "Only where the platform can do it: the process replaces itself, which needs exec, so it is refused on Windows with a reason naming what to do instead.",
        "responses": {
          "202": {
            "description": "Restarting"
          },
          "409": {
            "description": "The platform cannot restart in place"
          }
        }
      }
    },
    "/settings/ldap/sync/preview": {
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "What a directory synchronisation would do",
        "description": "Changes nothing, so an administrator can see which accounts and how many recorded entries a run would remove before committing to it.",
        "responses": {
          "200": {
            "description": "The report a run would produce"
          }
        }
      }
    },
    "/settings/ldap/sync": {
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Synchronise accounts with the directory",
        "description": "Matches on the directory-side identifier rather than the mail address, so a renamed mailbox is not read as a departure. `aborted` carries the reason a guard stopped the run.",
        "responses": {
          "200": {
            "description": "What the run did"
          }
        }
      }
    },
    "/admin/logs": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Read the process log",
        "description": "Behind the built-in administrator: the log carries email addresses, request paths and whatever a failing driver decided to print.",
        "parameters": [
          {
            "name": "since",
            "in": "query",
            "schema": {
              "type": "integer"
            },
            "description": "Only lines newer than this sequence number."
          },
          {
            "name": "levels",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Comma-separated, e.g. `WARN,ERROR`. Absent means every level."
          },
          {
            "name": "search",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Case-insensitive substring of the message."
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer",
              "maximum": 1000,
              "default": 300
            },
            "description": "How many lines at most; the newest are kept."
          }
        ],
        "responses": {
          "200": {
            "description": "Log lines, the levels that exist and how many were dropped"
          }
        }
      }
    },
    "/settings/update": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "What version is running, and whether a newer one exists",
        "description": "Answers on every deployment, and says which kind it is. `available` is the only field a button should key on: it means a newer version exists, it published a binary for this platform, this deployment is one where installing lasts, and it has not already been downloaded. `installable` is false in a container - a binary swapped inside one is undone by the next recreate, so the image is what gets updated there. `pending` names a version already on disk and waiting for a restart. A lookup that failed comes back as `problem` with a 200 rather than as an error: the release feed being unreachable is not this application being broken. Needs the rights the built-in administrator has, not merely settings:manage.",
        "responses": {
          "200": {
            "description": "The running version and what is available"
          },
          "403": {
            "description": "Not an administrator of this installation"
          }
        }
      },
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Download and install the newest release",
        "description": "Downloads the release's binary for this platform, checks it against the SHA256SUMS published beside it, and puts it where this process's own file is. Nothing is written into place until the hash matches - this is code that will be executed as the application on the next start. It does not restart: replacing the file and replacing the process are separate acts, and Windows cannot do the second at all, so the answer says which case this is. Refused in a container and when the check is switched off.",
        "responses": {
          "200": {
            "description": "In place; the answer says whether a restart is needed or possible"
          },
          "400": {
            "description": "Already current, switched off, or running in a container"
          },
          "403": {
            "description": "Not an administrator of this installation"
          },
          "500": {
            "description": "The download or the checksum failed"
          }
        }
      }
    },
    "/settings/update/check": {
      "post": {
        "tags": [
          "settings"
        ],
        "summary": "Ask the release feed now, rather than waiting out the cached answer",
        "description": "The version card is built from an answer the application keeps for six hours, because the release feed allows an unauthenticated caller sixty requests an hour per address and every open card would otherwise spend them. That is right for a screen refreshing itself and wrong for the minute after somebody publishes a release, when the card states a version that was newest this morning. This asks the feed at once and answers with the same body as `GET /settings/update`, so the card can be drawn again from what comes back. It has a limit of its own - once every five minutes for the whole installation - and refuses with `updateCheckedRecently` and the seconds remaining, so the button cannot hand back the problem the cache prevents. A feed that will not answer is reported as `problem` with a 200, exactly as the GET does. Needs the rights the built-in administrator has, not merely settings:manage.",
        "responses": {
          "201": {
            "description": "The running version and what is available, freshly asked"
          },
          "403": {
            "description": "Not an administrator of this installation"
          },
          "409": {
            "description": "Asked too recently, or the update check is switched off"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "description": "A personal API token. Not subject to the CSRF check, since a browser never sends this header on its own."
      },
      "apiToken": {
        "type": "apiKey",
        "in": "header",
        "name": "X-API-Token"
      },
      "sessionCookie": {
        "type": "apiKey",
        "in": "cookie",
        "name": "gtr_session",
        "description": "A browser session. State-changing calls made with this must also echo the gtr_csrf cookie in the X-CSRF-Token header and come from this origin; calls authenticated by a personal API token are exempt."
      },
      "csrfToken": {
        "type": "apiKey",
        "in": "header",
        "name": "X-CSRF-Token",
        "description": "The value of the gtr_csrf cookie, echoed back. Required alongside sessionCookie on POST, PUT, PATCH and DELETE."
      }
    },
    "schemas": {
      "TimesheetInput": {
        "type": "object",
        "required": [
          "date",
          "durationHours"
        ],
        "properties": {
          "userId": {
            "type": "integer",
            "description": "Defaults to the caller."
          },
          "projectId": {
            "type": "integer",
            "nullable": true,
            "description": "Optional; omit for uncategorised hours."
          },
          "date": {
            "type": "string",
            "format": "date"
          },
          "durationHours": {
            "type": "number",
            "minimum": 0.01,
            "maximum": 24
          },
          "description": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "ProjectInput": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "nullable": true
          },
          "startDate": {
            "type": "string",
            "format": "date"
          },
          "endDate": {
            "type": "string",
            "format": "date",
            "nullable": true
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "completed",
              "archived"
            ]
          }
        }
      },
      "DocumentTable": {
        "type": "object",
        "description": "A set of figures as the screen showed them.",
        "properties": {
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "numeric": {
            "type": "array",
            "items": {
              "type": "boolean"
            },
            "description": "Which columns are set flush right. The interface knows, because its own headings carry a class saying so."
          },
          "rows": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "DocumentSection": {
        "type": "object",
        "description": "One heading, with a chart and a table under it. Both are optional: the statistics screen is two charts and no table, and overtime is neither.",
        "properties": {
          "heading": {
            "type": "string"
          },
          "caption": {
            "type": "string"
          },
          "chart": {
            "type": "string",
            "description": "A PNG as a data URI, beginning `data:image/png;base64,`. Anything else is refused - a URL would be this server fetching whatever address a request named."
          },
          "table": {
            "$ref": "#/components/schemas/DocumentTable"
          }
        }
      },
      "DocumentRequest": {
        "type": "object",
        "required": [
          "title"
        ],
        "properties": {
          "title": {
            "type": "string"
          },
          "subtitle": {
            "type": "string",
            "description": "The period the evaluation covers, as the screen words it."
          },
          "sections": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DocumentSection"
            }
          },
          "summary": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "label": {
                  "type": "string"
                },
                "value": {
                  "type": "string"
                }
              }
            },
            "description": "The figures that stand on their own: a total, or the three numbers an overtime calculation comes to."
          }
        }
      }
    }
  }
}
